int minFlips(char * target){
    int sum = target[0] == '1' ? 1 :0;
    for(int i = 0;i < strlen(target) - 1;i++){
        if(target[i] != target[i + 1]){
            sum++;
        }
    }
    return sum;
}
